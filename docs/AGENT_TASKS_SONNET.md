# Agent Tasks — Claude Sonnet

Sonnet = UI/UX и контент. Аккуратные патчи, низкий риск. Работает ПОСЛЕ Codex по тем же файлам,
не конфликтует с логикой. Цель — Monobank/Duolingo/Apple-like ощущение, бренд `#ff1e1e`, без инфантильности.

## Жёсткие правила
- Только токены темы (`useTheme().colors`, `radii`, `spacing`, `shadows`) — НЕ хардкодить цвета,
  если это не текст на заведомо цветном фоне.
- Проверять КАЖДЫЙ экран в light и dark (ADB-тоггл: Профіль→Налаштування→Тема).
- Украинский основной, тексты без обрезаний; touch-таргеты ≥44px.
- Не менять бизнес-логику/Firestore; только разметка/стили/копия/анимации/empty-states.
- После: `npm run typecheck --workspace @lider/mobile`; ADB-скриншоты до/после.

## Очередь
1. **Главная — полировка** `index.tsx`: Тренажёр сделать «героем» (кольцо прогресса, «слабкі теми»);
   stagger-анимация появления карточек; haptics; бейдж «до екзамену N днів» на карточке учня.
2. **Рейтинг — визуальный пасс** (после рерайта Codex): типографика подиума, микроанимации, чистый solo-state.
3. **Empty-states везде**: чат, клуб-стрічка, нагороди, бронь — дружелюбно, с CTA, без «заглушек».
4. **Тексты UA** + микрокопия (кнопки, ошибки, подсказки Лідіка) — теплее, конкретнее.
5. **Темы spot-check** `tests.tsx` (45 хардкодов — найти нелегитимные на нейтральном фоне).
6. **Web** (опц.): hero/CTA/доверие/SEO-микрокопия в `apps/web` — только контент/стиль.

## ГОТОВЫЙ ПРОМТ (вставить в Sonnet) — ред. 2026-06-10 после приёмки Opus

> Контекст: Opus закоммитил `9f36600` (PDR game loop, duels, stats aggregation & UI updates). Gemini успішно оновив UI/UX для: Головного екрана (перетворено на дашборд з великими картками), Навчання (перетворено на покрокову дорожню карту з Лідіком), Рейтингу ПДР (empty states стали більш мотивуючими), Профілю (полагоджено "вічний бейдж 1"). Налаштовано бекенд-логіку для агрегації "Рейтингу ПДР" за тиждень/місяць і фільтри розблоковано. Тренажер ПДР має "Game Loop" (дуелі ПДР, слабкі теми). Ветка `opus/sprint-2-game-loop` зелёная по typecheck/lint/test. Сборка APK и ADB monkey (300 эвентов) прошли успешно без FATAL crash. Sonnet полирует визуал поверх этого.

```
Ты — Claude Sonnet, UI/UX-инженер проекта «Лідер» (Expo Router, RN, монорепо). Папка C:\Avtoschool_APP.
Бренд-красный #ff1e1e (токен colors.red).
Стиль: Monobank/Duolingo/Apple liquid, премиально, БЕЗ инфантильности и «дешёвого» вида.

ШАГ 0. Прочитай docs/MOBILE_UX_AUDIT_GEMINI.md, docs/MOBILE_UI_CHANGES_GEMINI.md, docs/LIDIK_GUIDE_DESIGN.md, docs/BUGS_AND_RISKS.md (особенно секцию «⛔ Приёмка Opus»), docs/CHANGELOG.md. git log --oneline -6. 
ЗАДАЧИ (только UI/стили/копия/анимации/empty-states — НИКАКОЙ бизнес-логики/Firestore/rules):
1) Темы и контраст: Пройди ВСЕ табы и под-экраны (tests/practice/assistant/booking/learning/profile) в light И dark. 
   - Найди и исправь нелегитимные хардкод-цвета на нейтральном фоне, переведи на токены (useTheme, colors).
   - Никакого белое-на-белом / красное-на-красном; UA-текст без обрезаний; touch-таргеты ≥44px.
2) Полировка Тренажера ПДР (tests.tsx) и Дуэлей: 
   - Добавь микроанимации и лоск в новый режим "Дуэль ПДР". Сделай так, чтобы прогресс-бар соперника выглядел напряженно и динамично (возможно, пульсация или градиент).
   - Проверь экран ResultScreen для дуэлей — победа/поражение должны выглядеть премиально и сочно (победа - радость, поражение - вызов).
3) LidikGuide (components/lidik-guide.tsx): 
   - Убедись, что он идеально вписывается в новую "Дорожную карту" (learning.tsx) и выглядит как единый премиальный компонент системы. 
   - Отполируй тени, отступы, треугольник "бульбашки", чтобы он не выглядел как дешевый CSS-хак.
4) Рейтинг ПДР (club.tsx): 
   - Визуальный пасс поверх подключенных фильтров времени (Тиждень/Місяць/Всі). Кнопки фильтров должны выглядеть как переключатели (segmented control) в iOS/Monobank.
5) Empty-states (чат/клуб-стрічка/нагороди/бронь): дружелюбно + CTA, без «заглушек».
6) UA-микрокопия: кнопки, ошибки, подсказки Лідіка — теплее, конкретнее.

ПРАВИЛА: только токены темы (colors/radii/spacing/shadows). Маленькие коммиты, conventional commits.
После КАЖДОГО экрана: npm run typecheck --workspace @lider/mobile (зелёный).
Источник истины — APK+ADB: после изменений переобери (assembleRelease, env EXPO_NO_METRO_WORKSPACE_ROOT=1 NODE_ENV=production) и проверь на устройстве, logcat без FATAL.

ОТЧЁТ: изменённые экраны, найденные смежные UI-баги, следующий промт. 
НЕ трогай: логику агрегации рейтинга, game loop, rules, booking-флоу, Crashlytics. Ищи визуальные баги активно, а не только заданные.
```
