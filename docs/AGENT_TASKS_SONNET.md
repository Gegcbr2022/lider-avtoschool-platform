# Agent Tasks — Claude Sonnet

Sonnet = UI/UX и контент. Аккуратные патчи, низкий риск. Работает ПОСЛЕ Codex по тем же файлам,
не конфликтует с логикой. Цель — Monobank/Duolingo/Apple-like ощущение, бренд `#ff1e1e`, без инфантильности.

## Жёсткие правила
- Только токены темы (`useTheme().colors`, `radii`, `spacing`, `shadows`) — НЕ хардкодить цвета,
  если это не текст на заведомо цветном фоне.
- Проверять КАЖДЫЙ экран в light и dark (ADB-тоггл: Профіль→Налаштування→Тема).
- Украинский основной, тексты без обрезаний; touch-таргеты ≥44px.
- Не менять бизнес-логику/Firestore; только разметка/стили/копия/анимации/empty-states.
- После: `npm run typecheck --workspace @lider/mobile`; ADB-скриншоты до/после.

## Очередь
1. **Главная — полировка** `index.tsx`: Тренажёр сделать «героем» (кольцо прогресса, «слабкі теми»);
   stagger-анимация появления карточек; haptics; бейдж «до екзамену N днів» на карточке учня.
2. **Рейтинг — визуальный пасс** (после рерайта Codex): типографика подиума, микроанимации, чистый solo-state.
3. **Empty-states везде**: чат, клуб-стрічка, нагороди, бронь — дружелюбно, с CTA, без «заглушек».
4. **Тексты UA** + микрокопия (кнопки, ошибки, подсказки Лідіка) — теплее, конкретнее.
5. **Темы spot-check** `tests.tsx` (45 хардкодов — найти нелегитимные на нейтральном фоне).
6. **Web** (опц.): hero/CTA/доверие/SEO-микрокопия в `apps/web` — только контент/стиль.

## ГОТОВЫЙ ПРОМТ (вставить в Sonnet) — ред. 2026-06-10 после приёмки Opus

> Контекст: Opus закоммитил `135c76d` (фикс бейджа), `eb6891f` (hero-редизайн главной: карточка-герой
> Тренажёра с полосой прогресса + 3 side-actions, stagger + haptics — УЖЕ ЕСТЬ, не переделывать с нуля),
> `e34e3fe` (Crashlytics). Рейтинг и бронь Codex'а закоммичены и верифицированы на устройстве. Ветка
> `codex/sprint-2-booking` зелёная по typecheck/lint/test. Sonnet полирует визуал поверх этого.

```
Ты — Claude Sonnet, UI/UX-инженер проекта «Лідер» (Expo Router, RN, монорепо). Папка C:\Avtoschool_APP.
Создай ветку sonnet/sprint-2-ui от codex/sprint-2-booking. Бренд-красный #ff1e1e (токен colors.red).
Стиль: Monobank/Duolingo/Apple liquid, премиально, БЕЗ инфантильности и «дешёвого» вида.

ШАГ 0. Прочитай docs/MOBILE_UX_AUDIT.md, docs/BUGS_AND_RISKS.md (особенно секцию «⛔ Приёмка Opus»),
docs/CHANGELOG.md. git log --oneline -6. Запусти APK на emulator-5554 (docs/ANDROID_QA_ADB.md),
скриншоты «до» в light И dark.

ЗАДАЧИ (только UI/стили/копия/анимации/empty-states — НИКАКОЙ бизнес-логики/Firestore/rules):
1) Главная app/(tabs)/index.tsx — hero Тренажёра уже есть (полоса прогресса + haptics + stagger). Доведи:
   а) опц. кольцо прогресса вместо полосы (или оставь полосу, но сделай её «дороже»);
   б) строка «слабкі теми: …» из реальных данных stats (если данных нет — мягкий нейтральный текст, НЕ мок);
   в) бейдж «До іспиту N днів» на карточке учня, если у профиля задана дата экзамена (иначе не показывать).
2) Рейтинг app/(tabs)/club.tsx LeaderboardView — визуальный пасс поверх рерайта Codex: типографика подиума,
   микроанимации появления строк, аккуратный sticky «Ваше місце». ВАЖНО: реши судьбу чипов Тиждень/Місяць —
   сейчас они не фильтруют (показывают all-time с disclaimer). Либо спрячь их до появления агрегатов, либо
   оставь, но сделай disclaimer заметнее. Карточку Лідіка (MascotMessage tone="error") в LIGHT-теме почисти —
   сейчас фон мутно-розово-бурый; добейся чистого премиального вида в обеих темах.
3) Темы: пройди ВСЕ табы и под-экраны (tests/practice/assistant/booking/learning/profile) в light И dark.
   Spot-check app/(tabs)/tests.tsx (~45 хардкод-цветов — найди нелегитимные на нейтральном фоне, переведи на токены).
   Никакого белое-на-белом / красное-на-красном; UA-текст без обрезаний; touch-таргеты ≥44px.
4) Empty-states (чат/клуб-стрічка/нагороди/бронь): дружелюбно + CTA, без «заглушек».
5) UA-микрокопия: кнопки, ошибки, подсказки Лідіка — теплее, конкретнее.

ПРАВИЛА: только токены темы (colors/radii/spacing/shadows). Маленькие коммиты, conventional commits,
Co-Authored-By Claude. После КАЖДОГО экрана: npm run typecheck --workspace @lider/mobile (зелёный).
Источник истины — APK+ADB: после изменений пересобери (assembleRelease, env EXPO_NO_METRO_WORKSPACE_ROOT=1
NODE_ENV=production) и проверь на устройстве, logcat без FATAL.

ОТЧЁТ: изменённые экраны, скрины до/после (light+dark), найденные смежные UI-баги, следующий промт.
НЕ трогай: рейтинг-логику, rules, booking-флоу, Crashlytics. Ищи визуальные баги активно, а не только заданные.
```
