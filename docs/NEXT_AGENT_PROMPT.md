# Наступний крок (оновлено Opus, 2026-06-11)

> ⚠️ Цей файл був застарілим. Завдання S2/S3, які тут описані, **вже виконані** на гілці
> `opus/sprint-2-game-loop`. Актуальний хендоф для виконавця — **`SONNET_NEXT_SESSION_PROMPT.md`**.

## Що ВЖЕ зроблено (перевірено в коді + typecheck exit 0)

1. ✅ **Game Loop «Тренажер ПДР»** (`tests.tsx`): тренування по темам, марафон, екзамен МВС (20 питань,
   таймер 20 хв), дуель ПДР (симульований суперник), «слабкі теми» через coach-план
   (`buildPdrCoachPlan`), AI-пояснення від Лідіка (3 стилі), візуальний розбір сцени.
2. ✅ **Агрегація рейтингу за тиждень/місяць**: `recordTestCompletion` пише `stats_<week>`/`stats_<month>`
   та `correct_<...>` через `increment`; `getLeaderboard(limit, timeWindow)` читає їх. Сегмент-контрол
   «Тиждень / Місяць / Всі» у `club.tsx` **розблоковано і підключено**.
3. ✅ **Головна** — дашборд з реальними `getUserStats`, hero-реплікою Лідіка, «Сьогодні для тебе».
4. ✅ **Навчання** — дорожня карта на **реальних** даних (Opus прибрав мок «Крок 1 з 4 / 25%»).
5. ✅ **Профіль** — фантомний бейдж «1» виправлено (фільтр `daily-test`/`streak` + скидання на фокус).

## Що лишилось (деталі — у `SONNET_NEXT_SESSION_PROMPT.md` та `BUGS_AND_RISKS.md`)

- 🔧 **B-16**: картки «Навчання» («Пробний іспит», «Тренування») ведуть в одне меню — додати deep-link
  параметри в `tests.tsx` (`?mode=exam|mistakes|topic`) і авто-старт режиму.
- 🔧 **B-17**: винести контекстні репліки Лідіка в `getContextualLidikTip()` (зараз хардкод по екранах).
- 👤 **Реліз** (дії власника): App Check debug-token, prod keystore, еквайринг, ротація ключів,
  фінальна збірка APK + `adb monkey` (див. `FIREBASE_SETUP_CHECKLIST.md`, `ANDROID_QA_ADB.md`).
